class Solution {
public:
    int minDistance(string word1, string word2) {
        // find longest common subsequence
        // the subsequence can be non-contiguous
        // then the cost will be
        // differnce of length + number of uncommon characters
    }
};